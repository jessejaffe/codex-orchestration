interface:
  display_name: "Codex Orchestration"
  short_description: "Credit savings without review spirals"
  default_prompt: "Use $codex-orchestration:orchestration to score once, use Terra High executive ownership below 5.0, hand real implementation to the cheaper score-selected model, require producer self-check and one executive acceptance check, allow one correction, then finish with the savings receipt."

policy:
  allow_implicit_invocation: true
