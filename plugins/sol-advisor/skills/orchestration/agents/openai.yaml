interface:
  display_name: "Sol Advisor Orchestration"
  short_description: "Sol/High directs, effort-scaled workers implement"
  default_prompt: "Use $orchestration with primary Sol / High directing and reviewing; route implementation 1.0–2.9 to Luna / Max, 3.0–5.0 to Terra / Medium, 5.1–6.5 to Terra / High, 6.6–7.9 to Sol / Medium, and 8.0–10.0 to a separate Sol / High implementer; prefix visible worker labels with Luna Max, Terra Medium, Terra High, Sol Medium, or Sol High; end completed tasks with only actual weekly usage, the all-Sol equivalent, and estimated routing savings; keep accepted fork changes on main; activate only when my current message directly says “Turn Sol Advisor on.”"

policy:
  allow_implicit_invocation: true
