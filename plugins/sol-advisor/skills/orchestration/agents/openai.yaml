interface:
  display_name: "Sol Advisor Orchestration"
  short_description: "Turn on Sol-led model routing and review"
  default_prompt: "Use $orchestration to manage chat-local state; activate only when my current message directly says “Turn Sol Advisor on.”"

policy:
  allow_implicit_invocation: true
