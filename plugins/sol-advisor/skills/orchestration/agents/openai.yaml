interface:
  display_name: "Sol Advisor Orchestration"
  short_description: "Sol/High directs, effort-scaled workers implement"
  default_prompt: "Use $orchestration with primary Sol / High directing and reviewing; route every scored task through the native Luna Max, Terra Medium, Terra High, Sol Medium, or Sol High band without separate worker authorization; preflight only the attempted tier with current-turn evidence and move only upward when unavailable; persist the exact one-decimal complexity score before work and always run the completion-gated three-line weekly savings receipt before the final answer; activate only when my current message directly says “Turn Sol Advisor on.”"

policy:
  allow_implicit_invocation: true
