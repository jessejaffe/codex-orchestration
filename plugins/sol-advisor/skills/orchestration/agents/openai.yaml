interface:
  display_name: "Sol Advisor Orchestration"
  short_description: "Minimize tokens and time with Sol-led routing"
  default_prompt: "Use $orchestration to route by complexity, keep accepted fork changes on main, and run the once-daily non-merging upstream audit; activate only when my current message directly says “Turn Sol Advisor on.”"

policy:
  allow_implicit_invocation: true
